1. Вопрос ментору по 6-й задаче: ссылка на диалог: 
https://js42online.slack.com/archives/C02LX9LR9JL/p1641647132009800
------------------------------------------------
РАБОТАЕТ в консоли но не прорисовывает стили:

const inputEl = document.querySelector("input");
const check = (ev) => {
  ev.currentTarget.value.length === Number(inputEl.dataset.length)
    ? inputEl.setAttribute("id", "validation-input.valid")
    : inputEl.setAttribute("id", "validation-input.invalid");
  console.log(ev.currentTarget.value.length);
  console.log(Number(inputEl.dataset.length));
  console.log(inputEl);
};
inputEl.addEventListener("blur", check);
-----------------------------------------------
РАБОТАЕТ:

const inputEl = document.querySelector("input");

const check = (event) => {
  return event.target.value.length == Number(inputEl.dataset.length)
    ? inputEl.setAttribute("class", "valid")
    : inputEl.setAttribute("class", "invalid");
};
inputEl.addEventListener("blur", check);
console.log(inputEl);

я не понял стили прописаны на id-селекторе, а работают на селекторе класса?